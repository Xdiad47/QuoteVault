name: quote_vault
description: "QuoteVault - A beautiful quote discovery and collection app with AI-powered features"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.0
  shared_preferences: ^2.5.4
  flutter_local_notifications: ^19.5.0
  share_plus: ^12.0.1
  path_provider: ^2.1.5
  cached_network_image: ^3.4.1
  flutter_svg: ^2.2.3
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
